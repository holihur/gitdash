# Consumer example: pull a private image from the gitdash OCI registry.
# Login first (plain HTTP needs the host in insecure-registries or HTTPS):
#   docker login <host> -u <user> -p <PAT>
# Build:
#   docker build -t consume-docker .
FROM <host>/<owner>/hello:1.0

# Point npm at the gitdash registry and authenticate with a PAT.
# Replace <host>, <owner> and <PAT>. The auth line must include the port.
registry=http://<host>/api/packages/npm/<owner>/
//<host>/:_authToken=<PAT>

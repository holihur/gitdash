---
title: "Package registry"
weight: 8
summary: "Private publish & install for npm / composer / pypi / rubygems / Go / cargo / Maven / Docker, plus apt / yum / apk / brew / snap system repositories."
---

A private package registry isolated by user/organization namespace, authenticated with a PAT (Basic auth).

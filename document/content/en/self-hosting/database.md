---
title: "Database"
weight: 3
summary: "SQLite by default; switch to PostgreSQL."
---

Embedded SQLite is the default (pure Go, no CGO). Switch to PostgreSQL with `GITDASH_DATABASE_URL`. See `docs/database.md` in the repository.

---
title: "Configure a BYOK key"
weight: 1
summary: "Add your model key and optional base URL/model."
---

Add a key under **Profile → BYOK**:

- `API key`
- Optional `Base URL` and `Model` (Anthropic/OpenAI-style endpoints)

The agent process receives the key via environment variables.
